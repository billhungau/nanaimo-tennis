import { createFileRoute } from "@tanstack/react-router";
import { PageIntro, SourceLink } from "@/components/page-elements";
import { timeline } from "@/lib/civic-data";
export const Route = createFileRoute("/timeline")({ head: () => ({ meta: [{ title: "Timeline | Indoor Tennis Nanaimo" }, { name: "description", content: "A sourced chronology of the Westwood Lake Tennis Club property acquisition, planned closure and public process." }, { property: "og:title", content: "Westwood indoor tennis timeline" }, { property: "og:description", content: "Key dates in the acquisition and future of the indoor tennis facility." }, { property: "og:type", content: "article" }, { property: "og:url", content: "https://nanaimo-tennis.lovable.app/timeline" }, { name: "twitter:card", content: "summary_large_image" }], links: [{ rel: "canonical", href: "https://nanaimo-tennis.lovable.app/timeline" }] }), component: TimelinePage });
function TimelinePage() { return <><PageIntro eyebrow="Timeline" title="A clear chronology of public decisions."><p>Dates below distinguish announcements and stated intentions from events that have already occurred.</p></PageIntro><section className="section-space"><div className="page-wrap max-w-4xl">{timeline.map((item, index) => <article key={item.date} className="grid grid-cols-[5rem_1fr] gap-5 border-b border-border py-8 sm:grid-cols-[9rem_1fr]"><div><p className="font-serif text-xl">{item.date}</p><p className="text-xs text-muted-foreground">{item.year}</p></div><div><div className="flex items-center gap-3"><span className="grid size-6 place-items-center rounded-full bg-secondary text-xs font-bold">{index + 1}</span><h2 className="font-serif text-2xl">{item.title}</h2></div><p className="mt-3 leading-7 text-muted-foreground">{item.text}</p>{item.source ? <div className="mt-3"><SourceLink href={item.source.url}/></div> : <p className="mt-3 text-xs font-medium text-muted-foreground">Primary documentation not yet publicly available</p>}</div></article>)}</div></section></>; }